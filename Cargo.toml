[package]
name = "focus"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[build-dependencies]
slint-build = "1.10"

[dependencies]
slint = { version = "1.10", features = ["backend-winit", "renderer-femtovg", "std"] }
